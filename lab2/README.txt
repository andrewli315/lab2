use 'makefile' to compile the three file such as bmi.h,bmi.cpp,and
bmi.cpp
I declare the class BMI in bmi.h
define the class with three member function: getdata(double,doubl)
setbmi()//with a return value and compare()
then i read inputfile and write outputfile in main.cpp
